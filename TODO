- check prequisites like write access to the kernel dir
- worry about umask
- check config option under cursor and show which distributions have it enabled (stats)
- optional libnotify integration?
- resume mode?
- convert menuconfig text to config elements
- investigate "unable to set symbol" if symbol recalculation fails
	- print expression manually and investigate e.g. dependencies
