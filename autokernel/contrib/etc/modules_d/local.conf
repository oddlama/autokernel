# Please create a proper local.conf for your system
# by using autokernel detect -o local.conf and review
# the resulting file (sort out what you don't want)

module local {
	assert $false: "Please create/generate a proper local.conf for your system. Alternatively, you may delete this statement to ignore this.";
}
